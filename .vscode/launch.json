{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch oidn_app",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/oidn_app.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "console": "integratedTerminal",
            "preLaunchTask": "CMake Build",
        }
    ]
}